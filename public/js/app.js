const s=document.getElementById('search'),cards=[...document.querySelectorAll('.card')],count=document.getElementById('count'),empty=document.getElementById('empty');function filter(){const q=s.value.trim().toLowerCase();let n=0;cards.forEach(c=>{const ok=!q||c.dataset.name.includes(q);c.style.display=ok?'grid':'none';if(ok)n++});count.textContent=n+' 个工具';empty.hidden=n!==0}s.addEventListener('input',filter);document.addEventListener('keydown',e=>{if(e.key==='/'&&document.activeElement!==s){e.preventDefault();s.focus()}if(e.key==='Escape'){s.value='';filter();s.blur()}});
